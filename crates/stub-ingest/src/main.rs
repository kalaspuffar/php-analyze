//! `stub-ingest` — Phase 3 placeholder.
//!
//! This binary exists today only so the Cargo workspace builds end-to-end.
//! The real ingest server (HTTP listener that accepts MessagePack batches
//! from `php-analyze`) lands in Phase 3 of `SPECIFICATION.md`.

fn main() {
    println!("php-analyze stub-ingest: Phase 3 placeholder; see SPECIFICATION.md §10.");
}
